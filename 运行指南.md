# UECD 项目运行指南

> Unleashing the Potential of Un-Interacted Exercises for Boosting Cognitive Diagnosis

基于 PyTorch 的认知诊断框架。完整流程：**环境准备 → 数据准备 →（可选）聚类 → 训练 → 测试**。

***

## 1. 环境准备

要求：Python >= 3.8（当前机器：Python 3.12.7）、PyTorch >= 1.12。

```powershell
# 建议使用虚拟环境
python -m venv venv
.\venv\Scripts\Activate.ps1

# 安装 CPU 版 PyTorch（无 GPU 时）
pip install torch numpy pandas scikit-learn tqdm

# 或安装 CUDA 版 PyTorch（有 NVIDIA 显卡时，以 CUDA 12.1 为例）
pip install torch --index-url https://download.pytorch.org/whl/cu121
pip install numpy pandas scikit-learn tqdm
```

验证 PyTorch 是否能识别 GPU：

```powershell
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```

***

## 2. 数据准备

代码按**相对路径**读取数据，所有命令都必须在项目根目录 `UECD/` 下执行。需要的文件：

| 文件                         | 用途              | 使用方            |
| -------------------------- | --------------- | -------------- |
| `data/neg_exer_ours.json`  | 训练集（含未交互习题采样结果） | train.py       |
| `data/Eedi/train_set.json` | 原始训练集（聚类用）      | cluster.py     |
| `data/Eedi/val_set.json`   | 验证集             | train.py 训练中验证 |
| `data/Eedi/test_set.json`  | 测试集             | predict.py     |

创建输出目录（训练/测试前必须存在，否则保存结果时报错）：

```powershell
mkdir data, model, result
```

> `time_logs/` 目录由 train.py 自动创建，无需手动建。

配置文件说明（格式：`学生数,习题数,知识点数`）：

- `config.txt`：训练时读取（当前 Eedi 配置：4163, 17746, 123）
- `config_Eedi.txt`：验证/测试时读取（17740, 8987, 286）
- `config_junyi.txt`：junyi 数据集备用配置

> 切换数据集时，注意同步修改 `data_loader.py` 中的数据路径以及对应的 config 文件。

***

## 3. （可选）学生聚类 —— 两阶段采样的第一阶段

对 `data/Eedi/train_set.json` 中的学生按知识点掌握向量做层次聚类（默认 50 簇），结果保存到 `data/Eedi/result50.json`：

```powershell
python cluster.py
```

> 若 `data/neg_exer_ours.json` 已由上游流程生成，可跳过此步。

***

## 4. 训练模型

命令格式：

```powershell
python train.py <device> <epoch>
```

- `device`：`cpu` 或 `cuda:0`（指定 GPU 编号）
- `epoch`：训练轮数，整数

常用命令：

```powershell
# 使用 0 号 GPU 训练 70 轮（论文设置）
python train.py cuda:0 70

# 无 GPU 时用 CPU 训练（建议先跑 2~5 轮验证流程）
python train.py cpu 5
```

训练过程中：

- 每轮结束自动在验证集上评估 accuracy / RMSE / AUC
- 模型快照保存为 `model/NCDM_UECD_epoch{1..N}`
- 验证指标追加写入 `result/NCDM_UECD_val.txt`
- 每轮耗时记录写入 `time_logs/training_time_NCDM_UECD_negloss_EAC.csv`
- 随机种子固定为 2024（在 train.py 末尾 `set_seed(2024)`）

其他可在 train.py 顶部调整的超参数：

```python
topk = 20        # 未交互习题采样数
batch_size = 256
# 优化器 Adam, lr=0.001；损失 = BCE + MSE + 0.1*BPR + QDCC
```

***

## 5. 测试模型

predict.py 无参数，会依次加载 `model/NCDM_UECD_epoch1` 到 `epoch100` 在测试集上评估：

```powershell
python predict.py
```

- 结果追加写入 `result/NCDM_UECD_test.txt`
- 强制在 CPU 上推理（代码内写死 `torch.device('cpu')`）
- 若训练不足 100 轮，找不到对应快照会报错；可修改 predict.py 中 `range(1, 101)` 为实际轮数，例如训练 5 轮改为 `range(1, 6)`，或直接指定 `epoch = 5`

***

## 6. 完整流程速查

```powershell
# 0. 进入项目目录
cd C:\Users\Administrator\Desktop\UECD

# 1. 安装依赖（首次）
pip install torch numpy pandas scikit-learn tqdm

# 2. 准备数据后创建输出目录（首次）
mkdir data, model, result

# 3. 聚类（可选）
python cluster.py

# 4. 训练
python train.py cuda:0 70     # GPU
python train.py cpu 5         # CPU 快速验证

# 5. 测试（先按实际训练轮数改 predict.py 的 range）
python predict.py
```

***

## 7. 常见问题

- **`FileNotFoundError: data/neg_exer_ours.json`**：数据未就位，按第 2 节放置数据文件。
- **`FileNotFoundError: model/NCDM_UECD_epochX`**：predict.py 测试的轮数超过了实际训练轮数，修改其循环范围。
- **`No module named 'CDM'`**：这是运行基线脚本 `NCDM.py` 才会遇到的问题（它 import 了仓库未包含的 CDM 模块）；主流程 train.py / predict.py 不受影响。
- **CUDA 显存不足**：改小 train.py 中的 `batch_size`，或改用 `python train.py cpu <epoch>`。
- **配置不匹配导致维度报错**：训练读 `config.txt`、验证/测试读 `config_Eedi.txt`，换数据集时两个文件都要与数据实际规模一致。

