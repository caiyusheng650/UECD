# UECD 命令速查（全量训练 + Sanity Check）

> 所有命令在项目根目录 `e:\UECD` 下执行，以下均为 **cmd** 命令。

---

## 1. 数据准备（全量训练首次需要）

```cmd
cd /d e:\UECD

:: 原始 Eedi CSV -> data/Eedi/{train,val,test}_set.json
python preprocess_eedi.py stats
python preprocess_eedi.py build

:: 学生聚类（两阶段采样第一阶段），生成 data/Eedi/result50.json
python cluster.py

:: 生成训练集 data/neg_exer_ours.json（含未交互习题采样）
python build_neg_exer.py

:: 创建输出目录（首次）
mkdir data
mkdir model
mkdir result
```

> 配置文件：训练读 `config.txt`，验证/测试读 `config_Eedi.txt`，两者当前均为 `17740,8978,281`（学生数,习题数,知识点数），换数据集时需同步修改。

---

## 2. 全量训练

命令格式：`python train.py <device> <epoch>`

```cmd
:: 0 号 GPU 全量训练 70 轮（论文设置）
python train.py cuda:0 70

:: 无 GPU / 快速验证流程（2~5 轮）
python train.py cpu 5
```

- 设备：`cpu` 或 `cuda:0`（指定 GPU 编号）；epoch 为整数
- 超参数：`topk=20`、`batch_size=256`、Adam `lr=0.001`
- 损失：`BCE(正例) + MSE(负例) + 0.1*BPR + QDCC`

输出位置：

| 产物 | 路径 |
| --- | --- |
| 每轮指标 | `experiments\run_<时间戳>\metrics.csv` |
| 汇总 | `experiments\run_<时间戳>\summary.txt` |
| 完整日志 | `experiments\run_<时间戳>\run.log` |
| 模型快照 | `model\NCDM_UECD_epoch{1..N}`（默认只保留最近 2 个） |

### 测试模型

```cmd
python predict.py
```

> predict.py 默认从 epoch1 测到 epoch100，若训练轮数不足需先改脚本里的循环范围。

---

## 3. Sanity Check（过拟合小数据集）

目的：在少量数据上快速判断模型/数据/训练管线是否合理 —— 若模型连小数据都记不住，说明有 bug，不要直接跑全量。

### 第 1 步：生成小数据集

```cmd
python build_overfit_small.py
```

- 默认抽取训练数据 `N=1024` 条 → `data\overfit_small.json`
- 默认抽取验证数据 `VAL_N=200` 组 → `data\overfit_val_small.json`
- 想改大小：

```cmd
set N=512
set VAL_N=100
python build_overfit_small.py
```

### 第 2 步：跑过拟合训练

```cmd
set UECD_TRAIN_FILE=data\overfit_small.json
set UECD_VAL_FILE=data\overfit_val_small.json
python train.py cuda:0 60
```

只关心训练损失、跳过验证（更快）：

```cmd
set UECD_TRAIN_FILE=data\overfit_small.json
set UECD_SKIP_VAL=1
python train.py cuda:0 60
```

### 第 3 步：跑完恢复环境变量（否则影响全量训练）

```cmd
set UECD_TRAIN_FILE=
set UECD_VAL_FILE=
set UECD_SKIP_VAL=
```

### 怎么判断结果

看控制台 `train_loss` 或 `experiments\run_<时间戳>\metrics.csv` 的 `avg_train_loss`：

- **合理**：train_loss 持续下降并逼近很小值（如 < 0.1），说明模型能记住小数据、管线无 bug，可以跑全量。
- **不合理**：loss 不降或卡在高位 → 检查模型、数据格式、损失、学习率。
- 参考：此前一次过拟合运行（60 轮约 1.65 分钟），loss 从 10.65 降到 1.06 且仍在下降，说明模型具备过拟合能力。

---

## 4. 10% 小规模试跑（正式训练前降风险）

担心全量训练白费时间时，先只拿 10% 数据训练看验证指标趋势。`build_overfit_small.py` 支持按比例抽取：

```cmd
:: 生成 10% 子集（训练 ~75,122 条 / 验证 1,774 组）
set FRAC=0.1
set VAL_FRAC=0.1
python build_overfit_small.py

:: 用 10% 数据训练 30 轮（约 293 batch/轮）
set UECD_TRAIN_FILE=data\overfit_small.json
set UECD_VAL_FILE=data\overfit_val_small.json
python train.py cuda:0 30

:: 跑完恢复
set FRAC=
set VAL_FRAC=
set UECD_TRAIN_FILE=
set UECD_VAL_FILE=
```

- `FRAC`/`VAL_FRAC` 只影响 `build_overfit_small.py` 的抽取比例；不设置时维持 `N`/`VAL_N` 的固定条数模式。
- 注意：10% 子集里学生/习题是随机抽的，验证集中必然有很多**没在训练中出现过**的学生/习题（embedding 未训练），所以验证指标会**偏低**，看的是"指标是否随轮数上升"的趋势，不是最终分数。
- 若 30 轮后 val acc/auc 明显上升且训练 loss 持续下降 → 全量训练值得跑；若指标纹丝不动 → 先回头排查模型/数据。

---

## 5. 预测 / 测试（predict.py）

在测试集上评测某个 run 的权重快照。默认自动选 `model/` 里**最新写入**的那次 run，也可手动指定：

```cmd
:: 直接跑（自动选最新 run，测它存在的所有轮次）
python predict.py

:: 手动指定某个 run（tag 就是训练输出里的 run 名，如 run_20260913_170000）
set UECD_PREDICT_RUN=run_20260913_170000
python predict.py

:: 跑完恢复
set UECD_PREDICT_RUN=
```

- 注意：predict 只测**实际存在**的快照（每次训练默认只保留最后 2 轮），不再扫 1~100。
- 结果在 `experiments\predict_<时间>\` 下的 `test_metrics.csv` 和 `summary.txt`。

---

## 6. 环境变量速查

| 变量 | 作用 | 默认值 |
| --- | --- | --- |
| `UECD_TRAIN_FILE` | 训练数据文件 | `data/neg_exer_ours.json` |
| `UECD_VAL_FILE` | 验证数据文件 | `data/Eedi/val_set.json` |
| `UECD_SKIP_VAL` | 设为 1 时跳过验证（sanity check 用） | 未设置 |
| `UECD_KEEP_CKPT` | 保留检查点数量 | 2 |
| `UECD_DEBUG` | 设为 1 时开启 CUDA 调试（极慢，仅排查 CUDA 错误用） | 未设置 |

> 在 PowerShell 中设置环境变量用 `$env:VAR="值"`，用法相同；cmd 用 `set`。
